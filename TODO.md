# TODO: Implement MongoDB Caching for Creative Idea Generator

- [x] Update requirements.txt to add pymongo
- [x] Create db.py with MongoDB connection and caching functions (get_cached_idea, set_cached_idea)
- [x] Modify main.py to import db functions and implement caching logic in /generate endpoint
- [ ] Install dependencies (pip install -r requirements.txt)
- [ ] Test the caching functionality by sending same topic multiple times
